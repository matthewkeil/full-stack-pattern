---
name: Publish Version

on:
  push:
    tags:
      - 'v*'
    branches:
      - master

jobs:
  PublishVersion:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github }}
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."

      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Install node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Extract branch name
        id: extract_branch
        shell: bash
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
      - run: echo github.ref is ${{ github.ref }}
      - run: echo branchName is ${{ steps.extract_branch.outputs.branch }}

      - run: npm ci
      - run: npm run build
      - run: npm run lint --if-present

      - run: npm publish
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}

      - run: npm install --prefix docs
        if: startsWith(github.ref, 'refs/tags/v')

      - run: npm run build:docs
        if: startsWith(github.ref, 'refs/tags/v')

      - name: cdk synth
        if: startsWith(github.ref, 'refs/tags/v')
        run: BRANCH=${{steps.extract_branch.outputs.branch}} npm run cdk -- synth
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.REGION }}

      - name: cdk deploy
        if: startsWith(github.ref, 'refs/tags/v')
        run: BRANCH=${{steps.extract_branch.outputs.branch}} npm run cdk -- deploy
        env:
          BRANCH: ${{ secrets.BRANCH }}
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.REGION }}
